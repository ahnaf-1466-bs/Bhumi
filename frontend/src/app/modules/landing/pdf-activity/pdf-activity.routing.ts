import { Route } from '@angular/router';
import { PdfActivityComponent } from './pdf-activity.component';

export const PdfActivityRoutes: Route[] = [
    {
        path: '',
        component: PdfActivityComponent,
    },
];
