import { Route } from '@angular/router';
import { FeedbackActivityComponent } from './feedback-activity.component';

export const FeedbackActivityRoutes: Route[] = [
    {
        path: '',
        component: FeedbackActivityComponent,
    },
];
